{
  "name": "theletterettes",
  "version": "1.0.0",
  "devDependencies": {
    "autoprefixer": "^7.2.5",
    "postcss-cli": "^4.1.1",
    "postcss-cssnext": "^3.1.0"
  },
  "scripts": {
    "build": "postcss source/*.css --use postcss-cssnext --dir .",
    "watch": "postcss source/*.css --use postcss-cssnext --dir . --watch"
  }
}
